import 'package:flutter/material.dart';

class HomeTermsWidget extends StatelessWidget {
  const HomeTermsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
